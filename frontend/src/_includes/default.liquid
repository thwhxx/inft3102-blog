<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="{{ '/css/style.css' | url }}">
    <title>{{title}}</title>
</head>
<body>
    <header>
      {% include './partials/header.liquid' %}
    </header>
    <main>
        <div class="container">
            {{ content }}
        </div>
    </main>
    <footer>
    {% include './partials/footer.liquid' %}
    </footer>
    <script nonce="{{ nonce }}" src="{{ '/js/contact.js' | url }}"></script>
</body>
</html>